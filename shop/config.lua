return {
    deviceId         = 'POS-1',
    deviceType       = 'pos',
    storeAccountName = 'Owner',   -- payments go here; this card enters config mode
    inventoryInput   = 'north',   -- transposer side items come from (ME interface)
    inventoryOutput  = 'south',   -- transposer side items go to (output chest)
}